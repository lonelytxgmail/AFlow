package com.aflow.common.model;

/**
 * Phase indicator for execution snapshots.
 */
public enum SnapshotPhase {
    /** Snapshot taken before node execution. */
    BEFORE,
    /** Snapshot taken after node execution. */
    AFTER
}
